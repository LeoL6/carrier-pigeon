#pragma once

#include "Message.h"

namespace Messages
{
  // void init();

  // void addIncoming(const char* text);
  // void addOutgoing(const char* text);

  // const Message& get(int index) const;
  // int count() const;

  // void appendInputChar(char c);
  // void backspace();

  // const char* getInputBuffer() const;
  // void clearInput();
};